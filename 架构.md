# AI Terminal Hub —— 架构设计（方案二 · 进程编排）

> 本文是 [`aihub_architecture_analysis.md`](aihub_architecture_analysis.md) 的落地续篇。
> 分析报告回答了"为什么旧路走不通"，本文回答"新路怎么走"。
>
> - 适用版本：ai-terminal-hub `v1.10.0`（Electron 40 + SolidJS + Vite + TS）
> - 采用方案：**方案二 / 路线 B —— 用非交互模式重写**
> - 平台：macOS（暂定），二进制路径见附录 A
> - 文档状态：✅ 已落地（2026-06-08）。本文是设计基线；代码已按此实现，旧路线 A（screen/AppleScript）已删除

> **实现状态**：路线 B 已全量替换路线 A。后端 `electron/{agents,runner,persistence,task,env}/` + 共享契约 `electron/ipc/contracts.ts`；前端 `src/hub/App.tsx`+`WorkspaceScreen.tsx` 改为 Run 模型 + 流式事件。旧 `electron/ipc/hub.ts`（1465 行）已删除。39 个测试通过，前后端 typecheck + 生产构建通过。运行：`npm run dev`。

---

## 0. 一句话定义

> **把"对三个终端做键盘 UI 自动化"换成"对三个 CLI 做进程级编排"。**
>
> 用户输入一次任务 → Hub 以非交互模式 `spawn` 三个 CLI → 实时回传 stdout/stderr → 用进程退出码而非屏幕文字判断成败 → 全程落盘可追溯。

这是当前架构唯一需要扭转的核心认知：**Claude / Gemini / Codex 都原生支持非交互（headless）模式，根本不需要去模拟人按键盘。**

---

## 1. 为什么是方案二（决策回顾）

分析报告把 6 类反复出错的根因归结为一句话：**把"终端模拟器控制"当成了"任务派发"在做。** 旧架构（路线 A）依赖 `screen readreg/paste/stuff/hardcopy` + AppleScript 剪贴板粘贴 + 模拟回车，这是最脆弱的一类东西——CLI 是为人交互设计的 TUI，有内部状态机、readline 缓冲、alt-buffer、trust prompt、bracketed paste……每一层都可能不按预期响应，"修一个漏一个"。

方案二把派发动作下沉到操作系统的进程边界：

| 维度 | 路线 A（UI 自动化，旧） | 路线 B（进程编排，新） |
|------|----------------------|----------------------|
| 送达机制 | 剪贴板粘贴 + 模拟 Cmd+V + 模拟回车 | argv / stdin 直接传入进程 |
| 送达确认 | 截屏 `hardcopy` + 正则猜测 | 进程已 spawn = 100% 收到 |
| 完成判断 | 抓屏文字找 "esc to interrupt" | 进程退出码 + 信号 |
| 就绪判断 | 抓屏文字找提示符 | 不需要——spawn 即就绪 |
| 失败定位 | "paste ok / enter ok" 误导 | 每阶段进程事件可定位 |
| 可观测性 | 几乎没有 | stdout/stderr/退出码全量落盘 |
| 抗 CLI 升级 | 极差（改 UI 就崩） | 强（只依赖公开 CLI 契约） |
| 环境隔离副作用 | `env -i` / 隔离 HOME 反噬 | 继承用户原生环境 |

**结论**：路线 A 永远不会真正稳定；路线 B 一次性把"是否送达、是否运行、是否完成、为何失败"四个问题用进程语义彻底解决。

---

## 2. 设计目标 / 非目标

### 2.1 目标（Must）
1. **可靠送达**：用户原话 100% 完整传给目标 CLI，不拆行、不丢字、不抢跑。
2. **可判定**：每个 AI 的运行处于一个明确的、由进程事件驱动的状态。
3. **可观测**：任意一次派发，事后都能查到"发了什么、给了谁、它说了什么、退出码多少、为什么失败"。
4. **可恢复**：单个 AI 失败/超时/被取消，不影响其他 AI，可单独重跑。
5. **不污染**：继承用户原生环境与登录态，不动账号配置，不留隔离副作用。
6. **可测试**：核心逻辑可在无 Electron、无真实 CLI 的条件下单测。

### 2.2 非目标（Won't，本期）
- ❌ 不做交互式多轮对话的 UI（多轮以"会话续接"实现，见 §6.7，但不复刻 TUI 体验）。
- ❌ 不再模拟键盘、不再截屏解析、不再用 `screen`、不再用 AppleScript 派发。
- ❌ 不做跨平台（先 macOS，Windows/Linux 留接口）。
- ❌ 不内置 API Key 直连（仍走各 CLI 自身的登录态，Hub 只编排进程）。

---

## 3. 架构总览

```
┌──────────────────────────────────────────────────────────────┐
│  Renderer（SolidJS）                                           │
│  HomeScreen → AiSelectScreen → WorkspaceScreen                 │
│   · 任务编辑器（prompt + 附件）                                │
│   · 派发面板：每个 AI 一张卡 = 状态 + 实时日志 + 退出码         │
│   · 取消 / 重跑 / 打开输出目录                                  │
└───────────────▲───────────────────────────┬──────────────────┘
                │ 事件流（webContents.send）  │ 调用（ipcRenderer.invoke）
   run:started/stdout/stderr/exit            run:dispatch / run:cancel / …
┌───────────────┴───────────────────────────▼──────────────────┐
│  Main（Electron 主进程） —— 编排核心                            │
│                                                                │
│  ┌────────────────┐   ┌─────────────────┐   ┌──────────────┐  │
│  │ IPC 注册/校验   │──▶│  Run 编排器      │──▶│ Agent 适配器 │  │
│  │ (register.ts)  │   │  (RunManager)   │   │  Registry    │  │
│  └────────────────┘   │  · 生命周期      │   │ claude/gemini│  │
│                       │  · 状态机        │   │ /codex 适配器│  │
│                       │  · 并发/超时/取消 │   └──────┬───────┘  │
│                       └────────┬─────────┘          │          │
│                                │ spawn(cmd,args,cwd) │ buildInvocation()
│                       ┌────────▼─────────┐  ┌────────▼───────┐  │
│                       │ ProcessRunner    │  │ 环境/二进制解析 │  │
│                       │ child_process    │  │ (env-resolver) │  │
│                       └────────┬─────────┘  └────────────────┘  │
│                                │ 事件 + 落盘                     │
│                       ┌────────▼─────────┐                      │
│                       │ Persistence      │  任务文件夹/runs/日志 │
│                       │ (run-store)      │  prompt_history.jsonl│
│                       └──────────────────┘                      │
└────────────────────────────────────────────────────────────────┘
                                │ child_process.spawn
                ┌───────────────┼───────────────┐
                ▼               ▼               ▼
        claude -p ...     gemini -p ...    codex exec ...
        （继承用户环境 / cwd=各自工作目录 / 登录态不变）
```

**分层职责**

| 层 | 职责 | 不负责 |
|----|------|--------|
| Renderer | 收集输入、展示状态与日志、触发派发/取消 | 不碰进程、不碰文件系统 |
| IPC 边界 | 参数校验、把渲染层意图翻译成编排调用 | 不含业务编排逻辑 |
| Run 编排器 | 一次派发的生命周期、状态机、并发、超时、取消 | 不知道具体 CLI 怎么调 |
| Agent 适配器 | 把"任务"翻译成某个 CLI 的 argv/stdin/解析 | 不管进程怎么跑 |
| ProcessRunner | 真正 spawn、流式收集、退出语义 | 不懂 CLI 语义 |
| 持久化 | 把每次 run 的输入/输出/元数据落盘 | 不做判定 |

---

## 4. 核心范式转变（旧 → 新）

| 旧概念（删除） | 新概念（替代） |
|--------------|--------------|
| `screen session` / `screenSessionName` | `Run`（一次进程执行）/ `runId` |
| `hardcopy` 截屏 + 正则就绪判断 | 无需就绪判断；spawn 即开始 |
| `readreg` + `paste` + bracketed paste | prompt 经 argv 或 stdin 传入 |
| `stuff '\r'` 模拟回车 + 5 秒重试 | 无回车概念；进程自行执行 |
| `getScreenSessionHealth` CPU/抓屏 | 进程 `exit`/`error` 事件 |
| trust prompt 抓屏 + `stuff '1\r'` | 适配器预置自主/信任参数 |
| AppleScript 打开 Terminal.app | 直接 `child_process.spawn`（Terminal 仅作可选"围观"入口） |
| `cleanupHubScreenSessions` 清理 51 个残留 | 进程结束即回收；可 `kill` 取消 |
| `lastSendResult.stages.{payloadPasted,submitOk}` | `RunRecord.{status,exitCode,signal}` |

---

## 5. 模块详解

### 5.1 Agent 适配器层（核心抽象）

**动机**：分析报告指出"每次 CLI 更新都可能再次破坏"。解法是把"某个 CLI 怎么以非交互方式被调用"收敛到**唯一一处**——适配器。CLI 改 flag，只改一个文件。

```ts
// electron/agents/types.ts
export type AutonomyMode = 'safe' | 'auto-edit' | 'full-auto';

export interface InvocationInput {
  prompt: string;            // 用户原话 + 附件/输出目录拼装后的最终文本
  workDir: string;           // 该 AI 的工作目录（cwd）
  autonomy: AutonomyMode;    // 自主程度（映射到各 CLI 的审批/sandbox 参数）
  model?: string;            // 可选模型覆盖
  resumeSessionId?: string;  // 可选：续接上一轮会话（见 §6.7）
  extraDirs?: string[];      // 可选：额外可访问目录（如附件所在目录）
}

export interface Invocation {
  command: string;                 // 绝对路径二进制
  args: string[];                  // argv
  stdin?: string;                  // 经 stdin 传入的内容（长文本走这里）
  cwd: string;
  env: NodeJS.ProcessEnv;          // 继承 + 必要覆盖
}

export interface ParsedEvent {
  kind: 'text' | 'tool' | 'usage' | 'session' | 'error' | 'raw';
  text?: string;
  sessionId?: string;              // 解析出的会话 id（供续接）
}

export interface AgentAdapter {
  id: 'claude-code' | 'gemini' | 'codex';
  displayName: string;
  /** 把一次任务翻译成具体进程调用 */
  buildInvocation(input: InvocationInput): Invocation;
  /** 可选：解析 stream-json/JSONL 为规范事件；不实现则按纯文本透传 */
  parseChunk?(chunk: string): ParsedEvent[];
}
```

**三个适配器的核心映射**（flag 已在本机核对，见附录 A）：

- **Claude 适配器**
  `claude -p <prompt> --output-format stream-json --verbose`
  - 自主：`safe → --permission-mode default`；`auto-edit → --permission-mode acceptEdits`；`full-auto → --dangerously-skip-permissions`
  - 续接：`--resume <sessionId>` 或 `--session-id <uuid>`
  - 附件目录：`--add-dir <dir>`；`cwd` = 该 AI 工作目录
  - 长 prompt 经 **stdin**（`--input-format text`），避免 argv 长度上限

- **Gemini 适配器**
  `gemini -p <prompt> -o stream-json`
  - 自主：`safe → --approval-mode default`；`auto-edit → --approval-mode auto_edit`；`full-auto → --approval-mode yolo`
  - 信任：`--skip-trust`（替代旧的抓屏确认 trust）
  - 续接：`--resume latest` / `--session-id <uuid>`
  - 附件目录：`--include-directories <dir>`；prompt 也可经 stdin 追加

- **Codex 适配器**
  `codex exec <prompt> --json --cd <workDir> --skip-git-repo-check`
  - 自主：`safe → --sandbox read-only`；`auto-edit → --sandbox workspace-write`；`full-auto → --dangerously-bypass-approvals-and-sandbox`
  - 续接：`codex exec resume --last`（或按 session id）
  - 最终结论：`--output-last-message <file>` 直接拿到最终回答
  - 长 prompt：传 `-`（从 stdin 读）

> 适配器只产出"调用计划"，不亲自跑进程——这让它可纯函数式单测（给定 input，断言 argv），完全不需要真实 CLI。

### 5.2 Run 编排器（生命周期 + 状态机）

一次"派发"= 用户一条任务扇出到 N 个 AI = N 个 `Run`。编排器拥有每个 Run 的生命周期。

**Run 状态机**（由进程事件驱动，不再靠抓屏）：

```
            dispatch()
queued ───────────────▶ spawning
                           │ spawn error
                           ├──────────────▶ failed(spawn)
                           │ spawned ok
                           ▼
                        running ──────┬──── exit code 0 ──▶ succeeded
                           │          ├──── exit code ≠0 ──▶ failed(exit)
                           │          └──── signal/kill ───▶ cancelled
                           │ timeout
                           └──────────────▶ timed_out (→ kill 进程树)
```

对照旧的 9 态 `HubSessionStatus`（`starting/ready/sending/running/stuck/...`），新状态全部有**客观进程依据**，不存在"stuck（卡住）"这种靠 CPU 阈值猜出来的模糊态。

```ts
// electron/runner/types.ts
export type RunStatus =
  | 'queued' | 'spawning' | 'running'
  | 'succeeded' | 'failed' | 'cancelled' | 'timed_out';

export interface RunRecord {
  runId: string;            // uuid
  dispatchId: string;       // 同一次"用户派发"的 N 个 run 共享
  agentId: AgentAdapter['id'];
  agentName: string;
  workDir: string;
  status: RunStatus;
  command: string;
  args: string[];
  pid?: number;
  exitCode: number | null;
  signal: NodeJS.Signals | null;
  startedAt: string;        // ISO
  finishedAt: string | null;
  durationMs: number | null;
  sessionId?: string;       // 解析出的会话 id（续接用）
  errorReason?: string;     // 失败时的人类可读原因
  logPaths: { stdout: string; stderr: string; meta: string; prompt: string };
}
```

**编排器职责**
- `dispatch(input)`：为每个勾选的 AI 建 Run，**并行** spawn（彼此独立，一个崩不影响另一个）。
- 并发上限可配置（默认 = 选中数量；可设上限避免机器过载）。
- **超时**：每个 Run 一个可配置超时（默认如 10 min），到点 `kill` 进程树并置 `timed_out`。
- **取消**：`cancel(runId)` → `tree-kill`(SIGTERM→SIGKILL)，置 `cancelled`。
- **重跑**：`rerun(runId)` 用同一 input 起新 Run（旧记录保留）。

### 5.3 ProcessRunner（进程执行原语）

唯一直接接触 `child_process` 的地方。

```ts
export interface ProcessRunner {
  start(inv: Invocation, hooks: {
    onStdout: (chunk: string) => void;
    onStderr: (chunk: string) => void;
    onExit: (code: number | null, signal: NodeJS.Signals | null) => void;
    onError: (err: Error) => void;
  }): { pid?: number; cancel: () => void };
}
```

要点：
- 用 `spawn`（**不经 login shell**），命令为**绝对路径**——绕开旧架构 `zsh -lic` 触发 conda 插件卡死的坑（分析报告时间线末期那个问题）。
- 长 prompt 写入 `child.stdin` 后 `end()`，不放 argv，规避 `E2BIG`/换行拆坏。
- `stdout/stderr` 设 `encoding utf8`，按 chunk 流式回调（既转发 UI，也写日志文件）。
- 取消用进程组 kill（`detached:true` + `process.kill(-pid)` 或 `tree-kill`），确保子孙进程一起回收。

### 5.4 持久化与可观测性

直接消灭分析报告根因 #6"没有可观测性"。每次 Run 在任务文件夹内留完整证据链（见 §7 目录结构）：

- `runs/<runId>/prompt.txt` —— 实际发给该 AI 的完整文本
- `runs/<runId>/stdout.log` / `stderr.log` —— 流式原样落盘
- `runs/<runId>/meta.json` —— `RunRecord` 快照（命令、argv、cwd、起止时间、退出码、信号、状态、sessionId）
- 任务级 `00_input/prompt_history.jsonl` —— 保留并增强（见 §6 数据模型）

意义：UI 不再显示误导性的 "paste ok / enter ok"，而是显示**进程真相**；排查时 `meta.json` + 两个 log 就能完全复盘。

### 5.5 环境与二进制解析

遵循分析报告的"该做 / 不该做"清单：

- ✅ **继承用户环境**：`env: { ...process.env, ...必要覆盖 }`，保留登录态、PATH、代理等。
- ✅ **绝对路径二进制**：复用现有 `agents.ts` 的可用性探测（`/bin/test -x`），spawn 时直接用绝对路径，必要时用一次 `printf "%s" "$PATH"` 解析（已有 `resolveCommandForSpawn`）。
- ❌ **不** `env -i` 清空环境；❌ **不**注入隔离的 `CODEX_HOME`/`GEMINI_CLI_HOME`/`HOME`（除非用户显式要求）。
- ❌ **不**强行套 `$SHELL -lic`——这正是 conda 钩子卡死的来源；改为直接 spawn 二进制。
- 覆盖项仅限确有必要者：`cwd`、可能的 `NO_COLOR=1`/`FORCE_COLOR=0`（让日志干净）、`CI=1`（部分 CLI 据此关交互）。

### 5.6 权限 / 自主模式（唯一需要权衡的地方）

非交互模式下，CLI 遇到需要审批的动作会**直接按策略走**而不是弹窗等人。因此自主程度必须显式声明，这是安全与"能干活"的权衡点。Hub 暴露三档（映射见 §5.1）：

| 档位 | 含义 | Claude | Gemini | Codex |
|------|------|--------|--------|-------|
| `safe`（默认） | 只读/只问，不擅自改盘 | `--permission-mode default` | `--approval-mode default` | `--sandbox read-only` |
| `auto-edit` | 自动改文件，不碰危险操作 | `acceptEdits` | `auto_edit` | `workspace-write` |
| `full-auto` | 全自动（仅限可信任务） | `--dangerously-skip-permissions` | `--approval-mode yolo` | `--dangerously-bypass-approvals-and-sandbox` |

- 默认 `safe`，由用户在 UI 显式升档，避免"无人值守却全自动改盘"。
- **工作目录信任**预置：Claude `--add-dir` + Gemini `--skip-trust` + Codex `--cd` + 工作目录在 sandbox 白名单内——一次性替代旧架构"抓屏发现 trust prompt 再 `stuff '1\r'`"的脆弱 hack。

### 5.7 会话连续性（一次性 → 可续接）

非交互 `-p` 天然是一次性的。要支持"接着上一轮说"，靠各 CLI 的 session 机制，而非常驻 TUI：

- 每个 Run 从输出里解析出 `sessionId`（Claude/Gemini 的 `--session-id`/stream-json 事件；Codex 的会话 id），写进 `RunRecord` 与按 AI 维度的 `session-state.json`。
- 下一次派发若用户选择"继续"，把 `resumeSessionId` 传给适配器（Claude `--resume`、Gemini `--resume`、Codex `exec resume`）。
- **分期**：Phase 1 先做无状态一次性派发（最稳）；Phase 3 再启用续接。

---

## 6. 数据模型

### 6.1 prompt_history.jsonl（保留并增强）

保留旧字段，把 `perAgentResults` 从"screen 阶段位"换成"进程结果"：

```jsonc
{
  "id": "uuid",
  "dispatchId": "uuid",
  "createdAt": "2026-06-08T...",
  "taskFolder": "/Users/.../督导异常查找",
  "userText": "用户真正输入的原话",
  "uploadedPaths": ["/path/to/file.xlsx"],
  "autonomy": "safe",
  "targetAgents": ["Claude Code", "Gemini CLI", "Codex CLI"],
  "sentTexts": ["实际发给 Claude 的最终文本", "..."],
  "perAgentResults": [
    {
      "runId": "uuid",
      "agentId": "claude-code",
      "agentName": "Claude Code",
      "status": "succeeded",
      "exitCode": 0,
      "signal": null,
      "durationMs": 48213,
      "sessionId": "....",
      "logPaths": { "stdout": "claude-code/runs/<id>/stdout.log", "...": "..." }
    }
  ]
}
```

### 6.2 前端会话模型（替换旧 `HubAgentSession`）

旧 `HubAgentSession` 携带 `screenSessionName/terminalTty/lastSendResult.stages.*`，新模型改为面向 Run：

```ts
export interface HubAgentRunView {
  agentId: string;
  name: string;
  workDir: string;
  checked: boolean;
  current: RunRecord | null;   // 当前/最近一次 run
  liveLog: string;             // 增量拼接的实时输出（UI 截断显示）
}
```

---

## 7. 任务文件夹结构

```
<任务文件夹>/
├── 00_input/
│   ├── uploaded_files/            # 用户拖入的附件（沿用现有逻辑）
│   ├── prompt_history.jsonl       # 任务级派发历史（增强）
│   └── session-state.json         # 各 AI 的可续接会话 id（Phase 3）
├── claude-code/                   # 各 AI 工作目录（= 进程 cwd）
│   └── runs/
│       └── <runId>/
│           ├── prompt.txt
│           ├── stdout.log
│           ├── stderr.log
│           └── meta.json
├── gemini-cli/
│   └── runs/<runId>/...
└── codex-cli/
    └── runs/<runId>/...
```

沿用现有 `prepareTaskFolder` / `ensureAgentWorkDir` / `importUploadedItems`——这部分文件管理逻辑是好的，**保留**。

---

## 8. IPC 契约

把"面向 screen 的多通道"收敛成"面向 run 的少数通道 + 事件流"。

**Renderer → Main（invoke，请求/响应）**

| 通道 | 入参 | 出参 |
|------|------|------|
| `run:dispatch` | `{ taskFolder, userText, uploadedPaths, autonomy, agents: [{agentId,name,folderName,workDir,model?}] }` | `{ dispatchId, runs: RunRecord[] }`（初始 queued 态） |
| `run:cancel` | `{ runId }` | `{ ok, status }` |
| `run:rerun` | `{ runId }` | `{ run: RunRecord }` |
| `run:list` | `{ taskFolder }` | `RunRecord[]`（历史回看） |

保留并复用：`hub_get_bootstrap`、`hub_create_task_folder`、`hub_prepare_task_folder`、`hub_import_uploaded_files`、`hub_delete_uploaded_file`、`hub_find_latest_non_log_file`、`hub_append_prompt_history`、`open_path`、`list_agents`、`hub_write_clipboard_text`。

**Main → Renderer（webContents.send，事件流）**

| 事件 | payload |
|------|---------|
| `run:started` | `{ runId, pid, startedAt }` |
| `run:stdout` | `{ runId, chunk }` |
| `run:stderr` | `{ runId, chunk }` |
| `run:event` | `{ runId, parsed: ParsedEvent }`（Phase 2，stream-json 解析后） |
| `run:exit` | `{ runId, status, exitCode, signal, durationMs, sessionId? }` |

**删除的通道**：`hub_launch_agent`、`hub_send_text_to_terminals`、`hub_cleanup_screen_session`、`hub_cleanup_all_screen_sessions`、`hub_cleanup_unhealthy_screen_sessions`、`hub_get_screen_session_health`。

> preload 需新增对事件通道的订阅桥（`ipcRenderer.on(channel, cb)` 经 contextBridge 暴露，仅白名单通道），当前 `preload.cjs` 只暴露了 `invoke`，需扩展。

---

## 9. 前端工作区重构

`WorkspaceScreen` 从"发送结果诊断表"变为"派发 + 实时结果"：

**保留**：顶部任务编辑器（textarea + 拖拽上传 + 发送）、附件条、复制输出目录/最新文件、打开目录。

**改造每张 AI 卡片**：
- 状态徽章：`排队中 / 启动中 / 运行中 / 完成 / 失败 / 已取消 / 超时`（对应 `RunStatus`）。
- **实时日志面板**（可折叠）：消费 `run:stdout/stderr` 增量追加——这就是分析报告说的"可选实时日志面板"。
- 退出码 + 用时；失败时显示 `errorReason` + 一键打开 `stderr.log`。
- 操作：`取消`（运行中）/ `重跑`（终态）/ `打开输出目录`。

**新增**：一个全局"自主模式"选择器（safe/auto-edit/full-auto，默认 safe）。

**删除**：`重启终端`、`screen: xxx`、`刷新状态`、`paste/clear/enter` 诊断行——这些都是 screen 时代的概念。

---

## 10. 失败语义（每一步可定位、可恢复）

直接对应分析报告"失败时必须能看出失败在哪一步"。失败被精确归到进程阶段：

| 失败点 | 检测方式 | 状态 | 用户可做 |
|--------|---------|------|---------|
| 二进制找不到 | spawn `ENOENT` | `failed(spawn)` | 检查路径配置 |
| 启动即崩 | 非 0 退出且无输出 | `failed(exit)` | 看 stderr.log |
| 跑到一半报错 | 非 0 退出码 | `failed(exit)` | 看 stdout/stderr |
| 卡死不返回 | 超时计时器 | `timed_out` | 已自动 kill，可重跑 |
| 用户主动停 | cancel → 信号 | `cancelled` | 重跑 |
| 正常完成 | 退出码 0 | `succeeded` | 看输出目录 |

**隔离性**：N 个 Run 是 N 个独立子进程，一个失败/超时不阻塞、不影响其余——满足"不影响别的 AI、不污染系统环境"。

---

## 11. 代码组织（建议目录）

遵循"多个小文件 > 少量大文件、按领域组织"。把现有 1465 行的 `hub.ts` 拆解：

```
electron/
├── main.ts
├── preload.cjs                 # 扩展：暴露事件订阅桥（白名单）
├── ipc/
│   ├── channels.ts             # 精简后的通道枚举
│   ├── register.ts             # 仅做校验 + 转调编排器
│   └── agents.ts               # 保留：二进制可用性探测
├── agents/                     # ★ 适配器层
│   ├── types.ts
│   ├── claude-adapter.ts
│   ├── gemini-adapter.ts
│   ├── codex-adapter.ts
│   └── registry.ts
├── runner/                     # ★ 编排核心
│   ├── types.ts                # RunRecord / RunStatus
│   ├── run-manager.ts          # 生命周期/并发/超时/取消
│   └── process-runner.ts       # child_process 封装
├── persistence/                # ★ 落盘
│   ├── run-store.ts            # runs/<id>/* 读写
│   └── prompt-history.ts       # jsonl（从 hub.ts 抽出）
├── task/                       # 任务文件夹（从 hub.ts 抽出，保留逻辑）
│   ├── task-folder.ts          # prepare/create/ensureWorkDir
│   └── uploads.ts              # import/delete/findLatest
└── env/
    └── resolver.ts             # 二进制路径/PATH/env 解析（从 hub.ts 抽出）
```

**删除**：`hub.ts` 中所有 `screen*` / `hardcopy` / `readreg` / `paste` / `stuff` / AppleScript 派发 / `classifyScreenTextForAgent` / `waitForAgentPromptReady` / `getScreenSessionHealth` / `cleanup*ScreenSessions` 等（约占该文件一半以上）。

---

## 12. 分阶段落地路线

| 阶段 | 目标 | 交付 | 验收 |
|------|------|------|------|
| **P1 单 AI 跑通** | 一个 AI（先 Claude）非交互 spawn + 流式回传 + 落盘 | `process-runner` + `claude-adapter` + `run:dispatch/exit` + 日志落盘 | 输入一句话，Claude 真跑、UI 见实时输出、退出码正确、`runs/<id>/` 齐全 |
| **P2 三幻神并行** | 三个适配器 + 并行编排 + 取消/超时/重跑 + 前端三卡 | `gemini/codex` 适配器 + `run-manager` 全功能 + `WorkspaceScreen` 重构 | 一条任务并发派发三个 AI，各自独立成败，可单独取消/重跑 |
| **P3 结构化与续接** | stream-json/JSONL 解析为事件 + 自主模式 UI + 会话续接 | `parseChunk` + `session-state.json` + 自主档位选择器 | 工具调用/用量结构化展示；"继续上一轮"可用 |
| **P4 清场** | 删除 screen/AppleScript 全部死代码 + 文档化 | 删除清单落实 + ADR | `grep -r screen|hardcopy|osascript` 在派发路径为空 |

### 测试策略（满足 80% 覆盖 + TDD）

- **单元（纯函数，无需 CLI）**：适配器 `buildInvocation`——给定 `InvocationInput`，断言 argv/stdin/env/cwd 完全正确（这是抗 CLI 升级的护城河，必须高覆盖）。
- **单元**：状态机迁移（spawn 错误/退出码/信号/超时 → 目标状态），用 fake `ProcessRunner` 注入事件。
- **集成**：用一个 `echo`/`cat` 桩"假 CLI"验证 ProcessRunner 流式收集 + 落盘 + 取消 kill。
- **契约**：对真实三 CLI 跑一个 `--help`/最小 prompt 冒烟（CI 可选、本地必跑），核对 flag 没漂移。
- **E2E（Playwright）**：home → 选 AI → 输入 → 派发 → 看到三卡状态流转。

---

## 13. 风险与权衡

| 风险 | 说明 | 应对 |
|------|------|------|
| CLI flag 漂移 | 三个 CLI 都在快速迭代，flag 可能变 | 全部收敛到适配器单文件 + 契约冒烟测试早发现 |
| 自主模式误伤 | `full-auto` 无人值守可能改坏盘 | 默认 `safe`；危险档需用户显式升档 + sandbox/工作目录边界 |
| 长 prompt | argv 长度上限 / 换行 | 一律走 stdin，不放 argv |
| 失去 TUI 实时交互感 | 非交互不能中途追问 | 实时日志面板 + 会话续接（P3）部分弥补；本就不是目标 |
| stream-json 格式差异 | 三 CLI 的结构化输出 schema 不同 | `parseChunk` 各自实现；P1/P2 先纯文本，P3 再结构化 |
| 进程树残留 | 子进程派生孙进程 | `detached` 进程组 + 组级 kill；退出即回收（远好于旧 screen 残留 51 个） |
| 登录态/网络 | CLI 依赖各自登录与代理 | 继承用户原生 env，不隔离；启动前可做一次登录态自检 |

---

## 14. 与旧架构的对照小结

- **送达**：剪贴板+按键 → argv/stdin ✅ 根因 #1/#2 消除
- **状态**：抓屏猜测 → 进程事件 ✅ 根因 #3 消除
- **隔离**：env -i/隔离 HOME 反噬 → 继承原生 env ✅ 根因 #4 消除
- **生命周期**：screen 残留累积 → 进程即起即收 ✅ 根因 #5 消除
- **可观测**：误导性阶段位 → 全量日志+meta 落盘 ✅ 根因 #6 消除

一句话：**方案二不是"修好" UI 自动化，而是从源头不再需要它。**

---

## 附录 A：三幻神非交互调用速查（本机已核对）

> 版本：Claude Code `2.1.168` · Gemini CLI `0.45.2` · Codex `0.137.0-alpha.4`

**Claude Code** — `/Users/yamijin/.local/bin/claude`
```bash
claude -p "任务文本" \
  --output-format stream-json --verbose \
  --add-dir <附件目录> \
  --permission-mode default          # auto-edit→acceptEdits / full-auto→--dangerously-skip-permissions
  # 续接：--resume <sessionId> | --session-id <uuid> | -c
  # cwd = 该 AI 工作目录；长文本经 stdin（--input-format text）
```

**Gemini CLI** — `/opt/homebrew/bin/gemini`
```bash
gemini -p "任务文本" \
  -o stream-json \
  --skip-trust \
  --include-directories <附件目录> \
  --approval-mode default            # auto_edit / yolo（= full-auto）
  # 续接：--resume latest | --session-id <uuid>
  # prompt 也会追加到 stdin
```

**Codex CLI** — `/Users/yamijin/.local/bin/codex`
```bash
codex exec "任务文本" \
  --json \
  --cd <该 AI 工作目录> \
  --skip-git-repo-check \
  --sandbox read-only \              # workspace-write / --dangerously-bypass-approvals-and-sandbox
  --output-last-message <最终回答文件>
  # 续接：codex exec resume --last
  # 长文本：用 "-" 从 stdin 读
```

可用性探测沿用 `agents.ts`：`/bin/test -x <绝对路径>`。

---

## 附录 B：术语表

| 术语 | 含义 |
|------|------|
| Dispatch（派发） | 用户一次任务，扇出到 N 个 AI |
| Run | 一个 AI 的一次进程执行；有唯一 `runId` |
| Adapter（适配器） | 把任务翻译成某 CLI 非交互调用的唯一模块 |
| Autonomy（自主模式） | safe / auto-edit / full-auto，映射到各 CLI 审批/sandbox 参数 |
| Headless / 非交互 | CLI 的 `-p`/`exec` 模式，跑完即退，不开 TUI |
| Resume（续接） | 用 session id 接着上一轮对话 |

---

_本文档为设计基线。实现过程中如与真实 CLI 行为冲突，以"修适配器单文件 + 补契约测试"的方式增量修订，并在此追加 ADR 记录。_
